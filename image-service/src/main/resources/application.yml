spring:
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    dev.buhanzaz.app: debug

minio:
  credentials:
    access-key: test
    secret-key: test
    endpoint: https//localhost:9000
    region: ru
